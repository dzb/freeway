# freeway-flow 并行分支上下文隔离方案

## 现状(2026-08)

当前实现:PARALLEL 分支**共享**同一个 `FlowContext` 与 `ExecState`。

- 数据区(`FlowContext.data`)是 `ConcurrentHashMap` — 单键读写线程安全。
- 执行态(`ExecState` 计数/栈)的复合操作已原子化 — 并发分支汇合到同一
  INCLUSIVE/LOOP 节点时,`peek→count→compare→pop` 在共享栈锁内执行,
  消除结构性竞态。
- **仍存在的限制**:并发写同一个 data key 时,最终值依赖线程调度
  (无事务性);回放/排障在多分支竞争场景下可能不稳定。
- 下文描述的 fork/overlay/join 完全隔离模型是设计提案,尚未实现。

上述现状在最近的 flow 语义修复(网关死路检测、表达式语义、$for 原子抢占等)
之后**保持不变**:分支依然共享同一个 `FlowContext` 实例,引擎的
`FlowEngineImpl.parallel_run_out()` 注释明确将"分支共享同一 FlowContext、
并发写同一 data key 是已知限制"列为既定行为并引用本文档;数据隔离
(per-branch overlay / join 合并)仍未实现。

## 背景

`freeway-flow` 当前在并行分支中共享同一个 `FlowContext`。虽然底层使用了并发容器，但这只能保证容器层面的线程安全，不能保证流程语义上的一致性。

真正的问题不是“会不会崩”，而是：

- 分支 A 和分支 B 可能同时写同一个 key，最终结果依赖线程调度。
- 一个分支可能读到另一个分支刚写入但尚未完成的中间态。
- 回放、排障和测试都很难稳定复现。

如果希望调用方完全不需要关心线程安全，推荐把并行分支设计成“隔离执行 + 汇合提交”。

## 目标

- 调用方不需要处理线程安全。
- 并行分支之间默认互不影响。
- 并行执行结果可预测、可回放、可测试。
- 对确实需要共享状态的场景，提供显式的共享机制，而不是隐式共享整个 `FlowContext`。

## 建议的模型

### 1. 拆分运行态与业务数据

建议把上下文拆成两层：

- `FlowRunState`：共享执行态
- `FlowContext`：分支级业务数据

共享执行态建议保留：

- `trace`
- `eventBus`
- `stopped` / `interrupted`
- `stepCount`
- `temporary`

业务数据则不直接共享 `ConcurrentHashMap`，而是给每个并行分支一份视图。

### 2. 分支上下文使用 fork / overlay

进入并行分支时，为每个分支生成一个子上下文：

- 读取时先查本分支的 overlay
- 找不到时再回退到父快照
- 写入只落到本分支 overlay

这样可以保证：

- 分支之间默认隔离
- 分支内读写语义稳定
- 调试时可以准确看到每个分支修改了什么

### 3. join 时显式 merge

当所有并行分支执行结束后，由引擎在 join 点统一合并结果：

- 单分支修改的 key，直接合并回父上下文
- 多分支修改同一个 key，默认报冲突
- 如确有需要，可为特定 key 配置 merge 策略

默认策略建议是 fail fast，而不是“最后写入覆盖前面的写入”。

## 兼容性建议

### 兼容现有流程控制

以下对象建议继续共享：

- `trace`
- `eventBus`
- `stopped` / `interrupted`
- `temporary`

原因是这些对象描述的是“本次运行”，而不是某个分支独占的业务数据。

### 保留显式共享能力

对于确实需要跨分支协调的数据，不建议继续塞进主 `FlowContext` 数据区。

更好的方式是单独暴露一个显式共享对象，例如：

- `SharedState`
- `BranchMailbox`
- `ConcurrentState`

这样语义更清楚，也能避免把所有场景都拖进共享内存模型。

## 实施建议

### 第一阶段

- 保持现有 API 不变。
- 为并行分支增加上下文 fork 机制。
- 默认只隔离 `data`，共享执行态。

### 第二阶段

- 增加 join 合并策略。
- 先支持基础 merge，再支持冲突检测。

### 第三阶段

- 引入显式共享状态 API。
- 为需要跨分支协作的场景提供专用接口。

## 测试建议

建议补以下回归测试：

- 并行分支各自写入不同 key，join 后都能保留。
- 并行分支写同一个 key，默认报冲突。
- 分支内读取只能看到自己的写入和父快照。
- 并行分支执行顺序变化时，结果仍然稳定。

## 结论

并行分支共享同一个 `FlowContext` 并不是最优设计。  
如果目标是让调用方完全不用考虑线程安全，最稳妥的方案是：

- 执行态共享
- 业务数据分支隔离
- join 时统一合并
- 冲突显式处理

这比单纯使用并发容器更符合框架型产品的开发体验。
