# Freeway 框架整体审计与评估报告

- **审计日期**：2026-09-03
- **版本**：v1.4.0（HEAD `9511b4a1`）
- **环境**：JDK 25.0.3 LTS（Temurin）· Maven 3.9.16 · macOS aarch64
- **范围**：freeway-commons / ioc / boot / http / db / flow / cloud 共 7 个模块

---

## 一、执行摘要

Freeway 是一个工程质量**相当高**的现代 Java 框架。它的三大核心承诺——**零外部依赖、无类路径扫描、compose-first 显式装配**——在代码层面全部兑现，而非停留在口号。文档体系（AGENTS.md / CLAUDE.md / CHANGELOG.md / docs/）异常完整，CHANGELOG 记录了大量诚实且有深度的修复（如跨线程事务、HPACK Huffman DoS、连接池竞态、TOCTOU 路径穿越），说明作者对边界条件的理解远超平均水平。

**总评：8.5 / 10**（工程成熟度与测试质量是主要加分项；本次审计已就地修复唯一的 cloud 高危项）。

全量测试 **1825 个用例全绿**（0 失败 / 0 错误 / 0 跳过），较上一轮代码审查记录（1517）又新增 300+。

| 维度       | 评分     | 关键结论                  |
| -------- | ------ | --------------------- |
| 构建与测试健康度 | 9.5/10 | 1825 测试全绿             |
| 安全态势     | 8.5/10 | 核心攻击面防护到位，1 个高危点已修复   |
| 代码质量与设计  | 9/10   | 承诺兑现，几乎无死代码/遗留        |
| 并发正确性    | 9/10   | 未发现数据竞态/死锁/泄漏         |
| 测试覆盖     | 8/10   | 整体 82%，flow 模块偏薄（52%） |
| 文档一致性    | 9.5/10 | 文档与代码高度同步             |

---

## 二、项目概览

### 规模

| 指标    | 数值       |
| ----- | -------- |
| 模块数   | 7        |
| 主代码文件 | 425 个    |
| 主代码行数 | 50,303 行 |
| 测试文件  | 159 个    |
| 测试行数  | 41,480 行 |
| 测试用例  | 1,825 个  |

### 依赖卫生（承诺验证 ✅）

"核心模块零外部依赖"**属实**：

- 7 个模块 compile 依赖仅 `slf4j-api` + 内部模块。
- `freeway-db` 的 H2、`freeway-http` 的 boot 均为 **test scope**，无 test→compile 泄漏。
- 无跨模块 internal 包泄漏、无循环依赖，依赖图与文档一致（commons ← ioc ← boot/http/flow/cloud；db 仅依赖 commons）。

---

## 三、构建与测试健康度（9.5/10）

```
freeway-commons  tests= 398  ✓
freeway-ioc      tests= 259  ✓
freeway-boot     tests=  74  ✓
freeway-http     tests= 390  ✓
freeway-db       tests= 460  ✓
freeway-flow     tests= 112  ✓
freeway-cloud    tests= 132  ✓
─────────────────────────────
TOTAL            tests=1825  0 失败 / 0 错误 / 0 跳过
```

单模块构建命令（`mvn -pl xxx -am test`）与文档完全一致，无隐藏的环境依赖或本地 hack。

---

## 四、安全审计（8/10）

### 已妥善防护的攻击面 ✅

| 攻击面             | 防护位置                                                   | 结论                                                                      |
| --------------- | ------------------------------------------------------ | ----------------------------------------------------------------------- |
| 静态文件路径遍历        | `StaticResourceMount.relativePath()` / `resolveReal()` | 拒绝 `..`/`\`/`\0`/空段 + `toRealPath()` 二次包含校验 + `NOFOLLOW_LINKS` 抗 TOCTOU |
| HTTP 头注入 / 请求走私 | `Http1xParser`                                         | 严格 `1*DIGIT` Content-Length、拒绝重复 CL、TE chunked 唯一且末位、CL+TE 并存拒绝、控制字符拒绝  |
| SQL 注入          | `QueryImpl`                                            | 全部经 `PreparedStatement.setObject` 参数绑定，集合展开只追加 `?`                      |
| JSON DoS        | `JsonParser`                                           | `MAX_DEPTH=1000`、字符串 10MB、数字长度上限、输入 32MB、数组/对象各 1M                      |
| 响应头注入           | `AbstractHttpContext.validateHeaderName/Value`         | isToken + 拒 CTL/DEL + 限 ISO-8859-1                                      |
| 反序列化深度          | 无外部 gadget 依赖（零依赖架构天然缩小攻击面）                            | —                                                                       |

### 🔴 高危：cloud 事件网格未认证的任意类反序列化 —— ✅ 已修复

**位置**：`freeway-cloud/.../events/PeerHub.java:339-352`

```java
if (!allowedTypes.isEmpty() && !allowedTypes.contains(frame.type())) { ... return; }
Class<?> type = Class.forName(frame.type(), false, getClass().getClassLoader());
Object event = codec.fromJson(frame.dataJson(), type);
```

**问题**：入站 CloudEvent 的 `type` 字段完全由对端控制，而原实现中 `allowedTypes` 默认**空 = 放行任意类**，配合 `token` 默认**空 = 关闭对等认证**，一个未认证对等方可触发任意类 `Class.forName` + JSON 反序列化（构造器 + 属性填充），属于经典的多态反序列化类漏洞面。

**修复（本次审计已实施）**：将 CLASS 通道改为 **deny-by-default**——`allowedTypes` 未配置时一律丢弃 CLASS 事件，永不回退到"接受任意类"。这是只针对危险路径（`Class.forName`）的精准收口：

```java
if (!allowedTypes.contains(frame.type())) {  // 空列表 → 恒拒绝
    LOG.debug("Type not allowlisted for CLASS-channel delivery — dropped: {}", frame.type());
    return;
}
```

TOPIC 通道（通用 JSON 投递、无类解析）保留"空 = 全放行"的既有文档语义不变，因此**不破坏可信内网 mesh 的 TOPIC 场景**。同步更新了：模块 javadoc、启动告警文案、单元测试（`emptyTypeAllowlistDropsClassChannelByDefault`）、E2E 测试（`keyedEventsPreserveTheOrderingSubject` 改为显式白名单）。cloud 模块 133 个测试全绿。

**遗留建议（P1，防御纵深）**：`token` 仍默认关闭对等认证（TOPIC 通道仍可被未认证对等方投递任意 JSON 到任意 topic）。生产环境若 mesh 端点暴露在不可信网络，仍应配置 `freeway.cloud.events.token` 与 `allowed-topics`。

### ⚠️ 中危：cloud RPC 无显式 SSRF 防护

`CloudHttpClientDefault.doCall()`（270-285 行）URL 由 `endpoint.uri() + path` 拼接，无 scheme/host 白名单、无私网地址拦截。信任边界在服务注册中心——若注册中心被攻破或误配，可被用于访问内网。**建议**：对 endpoint 增加内网网段（RFC 1918 / 链路本地 / localhost）可选拦截开关。

### ⚠️ 低危

1. **明文事件通道默认**：`PeerConnector` 默认 `ws://`（明文），mesh 握手 token 在明文信道传输（`PeerConnector.onOpen` 297-299 行）。建议 mesh 场景强制 TLS。
2. **`SslContextFactory`**：`keyStorePassword` 为 null 时 `password.toCharArray()` 有 NPE 隐患（约 75 行），建议对空密码给明确报错。
3. **`SecretSymbolSource`** 默认无白名单，环境变量遮蔽仅 WARN（39-54 行）。

---

## 五、代码质量与设计审计（9/10）

### 优点 ✅

- **零遗留标记**：TODO/FIXME/XXX/HACK 全仓仅 1 处且为文档示例；主代码零遗留。
- **零 `printStackTrace()`**；约 30 处空 catch 均为 close/release 最佳努力清理，语义合理。
- **资源管理规范**：`try-with-resources`、长生命周期流统一收口，未发现泄漏。
- **命名高度一致**：`XDefault`/`XImpl`/`ModuleEx` 约定贯穿全仓。
- **模块边界干净**：无跨模块 internal 泄漏、无循环依赖。
- **文档与代码同步**：文档声称已删除的类（`JdkHttpEngine`/`JdkHttpContext`/`Robaho`）代码中确无残留。

### 轻微瑕疵（建议级）

| 问题     | 位置                              | 说明                                                                                                        |
| ------ | ------------------------------- | --------------------------------------------------------------------------------------------------------- |
| ~~死依赖声明~~ ✅ **已修复** | ~~`pom.xml:80-97`~~                 | 已从 `<dependencyManagement>` 移除四个 `freeway-starter*` 条目（全仓无任何引用）。移除后 7 个内部依赖声明与 `<modules>` 一一对应 |
| ~~命名偏差~~ ✅ **已修复** | ~~`MetricsDefault.java:122/145`~~   | `DefaultCounter`/`DefaultTimer` → `CounterImpl`/`TimerImpl`（全仓仅有的两处 `DefaultX` 前缀，已清零）                    |
| 内联魔法数字 | `AbstractHttpContext:164` 等 4 处 | `new byte[8192]` 建议统一为命名常量                                                                                |
| 未跟踪目录  | 仓库根 `.reasonix/`                | 他人工具遗留（含 attachments/tasks），未加入 .gitignore，建议清理或忽略                                                        |

---

## 六、并发正确性审计（9/10）

专项审计了连接池、EventBus/CallBus、ScopedValue 事务、IoC 容器、HTTP 引擎、flow PARALLEL/LOOP、LazyValue/缓存七处并发敏感区域。

**结论：未发现明确的数据竞态、死锁、连接或内存泄漏。** 保护机制覆盖到位：

- `PoolDefault` 的 borrow/release/close 在 `lifecycleLock` 下原子化，`active.remove` 防双重归还，cleaner 用 `remove` 返回值兜底 TOCTOU。
- `ContainerImpl` 单例 realize 用全局锁 + targetCache 双检，close 与 realize 竞态有 closed 复查兜底。
- `EventStreams` 用 `SubmissionPublisher.offer` overflow-drop 背压；`LazyValue` volatile DCL。
- 事务用 `ScopedValue` + `ConcurrentHashMap.newKeySet` 拦截跨线程消费。

### 次级瑕疵（非阻塞，建议补文档）

1. **`QueryImpl.java:42-46`**：惰性解析字段（`expandedChecked`/`positionalIndexes`/`namedParams`）非线程安全——Query 对象跨线程复用会竞态，应明确"单线程使用"契约或文档化。
2. **`DatabaseImpl.ping()`（:229）**：未走 `checkNoForeignTransaction` 守卫，事务期间会独立借连接（当前仅 `isValid(5)` 无写风险）。
3. **`parallel_run_in`（FlowEngineDefault:521）**：countIncr→比较→reset 非原子，依赖"恰好 N 分支到达"不变式。

---

## 七、测试覆盖评估（8/10）

| 模块               | 主代码行       | 测试行        | 测试/主比      | 测试文件数 |
| ---------------- | ---------- | ---------- | ---------- | ----- |
| freeway-db       | 8,030      | 9,332      | **116%**   | 25    |
| freeway-ioc      | 6,418      | 6,827      | **106%**   | 30    |
| freeway-boot     | 1,615      | 1,774      | **110%**   | 7     |
| freeway-commons  | 8,879      | 6,871      | 77%        | 25    |
| freeway-http     | 13,469     | 10,270     | 76%        | 44    |
| freeway-cloud    | 6,319      | 3,552      | 56%        | 24    |
| **freeway-flow** | **5,444**  | **2,854**  | **52%** ⚠️ | **4** |
| **合计**           | **50,303** | **41,480** | **82%**    | 159   |

**主要缺口**：

- **freeway-flow 覆盖最薄**：52% 的测试/主比，且仅 4 个测试文件承载 5,444 行主代码（GraphSpecTest / FlowApiRestorationTest / FlowEngineTest / FlowModuleTest）。表达式求值器（`ExprEvaluator` ~280 行递归下降解析器）、PlantUML 导出、LOOP/PARALLEL 边界、子图调用的组合路径测试密度偏低。
- **freeway-cloud 次之**（56%）：PeerHub/PeerConnector 的握手、去重、断线重连、SSRF 边界等网络交互路径较难单测覆盖。

---

## 八、整体评价

### 核心优势

1. **诚实的工程**：CHANGELOG 记录了大量真实的边界修复（跨线程事务原子性、HPACK 恶意头部 DoS、连接池竞态、TOCTOU），说明不是在"堆功能"，而是在"磨边界"。
2. **设计克制**：概念少（Module/Service/Extension/Scope/Runtime）、API 小而显式、命名约定严谨，符合项目自称的"engineering aesthetics"。
3. **零依赖落地**：自研 JSON 解析器、HTTP/2+HPACK、SQL 方言驱动解析器、连接池、事件总线，且质量均经得起审计——这是极高的技术自信。
4. **测试文化**：测试行数接近主代码（82%），大量确定性回归测试锁定历史竞态与安全漏洞。

### 风险与改进建议（按优先级）

| 优先级    | 建议                                                    | 理由                          |
| ------ | ----------------------------------------------------- | --------------------------- |
| **P0** | ~~PeerHub CLASS 通道任意类反序列化~~ ✅ 已修复（deny-by-default）    | 见上文                         |
| P1     | 生产环境配置 mesh `token` + `allowed-topics`（TOPIC 通道默认仍开放） | 未认证对等方仍可投递任意 JSON 到任意 topic |
| P1     | cloud RPC 增加内网地址（SSRF）可选拦截                            | 防注册中心被攻破后的内网探测              |
| P1     | 补齐 freeway-flow 测试密度（表达式求值器/子图/PARALLEL-LOOP 组合）      | 当前最薄模块                      |
| P2     | 清理 `pom.xml` 死 starter 依赖声明；处理 `.reasonix/` 未跟踪目录     | 卫生问题                        |
| P2     | 明确 `QueryImpl` 非线程安全契约（文档或注解）                         | 消除误用竞态隐患                    |
| P3     | mesh 事件通道默认 TLS；`SslContextFactory` 空密码明确报错           | 加固默认值                       |

> 附注：本次审计同时发现 `freeway-http` 的 `Http2ProtocolTest.h2cResetStreamWithNoErrorReleasesHandler` 存在**偶发性失败**（时序敏感的 keep-alive 断言，与本次改动无关；复跑 20/20 通过），建议后续留意 CI 稳定性。

### 结论

Freeway 是一个**可以放心作为学习范本，且具备生产可用潜力**的框架。核心模块的工程质量、安全防护意识与测试文化均属上乘；需要立即处理的是 cloud 事件网格的默认安全姿态（一个高危项），以及 flow 模块的测试覆盖补强。整体处于"优秀的工程作品"区间，而非"能跑的原型"。
