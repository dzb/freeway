# H2 in-memory database — swap the three keys below for PostgreSQL/MySQL
# credentials and the rest of the demo is unchanged.
freeway.db.url=jdbc:h2:mem:rest_db;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
freeway.db.username=sa
freeway.db.password=
