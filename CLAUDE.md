# CLAUDE.md

The agent contract is canonical in [AGENTS.md](AGENTS.md) (conventions) and
[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) (module boundaries and internals).
It is imported below rather than restated, so Claude Code reads exactly the
file every other agent reads.

@AGENTS.md
